[package]
name = "driver-codex"
version = "0.1.0"
edition.workspace = true

[dependencies]
shared-types = { path = "../shared-types" }
terminal-viewport = { path = "../terminal-viewport" }

[dev-dependencies]
serde_json.workspace = true
